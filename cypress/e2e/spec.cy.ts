// cypress/e2e/add-members-dialog.cy.ts

describe('AddMembersDialog E2E', () => {
  it('should open the dialog, add a member, and close', () => {
    // 1) Navigate to page where the AddMembers button is
    cy.visit('/verify-email'); // or whatever route displays your button
 
});
});
